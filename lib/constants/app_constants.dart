String defaultType = "none"; // pomodoro / break / none
String defaultStatus = "stop"; // play / pause / stop
int defaultStarted = 0; // milliseconds since epoch
int defaultRemain = 0 * 60; // seconds
int defaultPomodoro = 25 * 60; // seconds
int defaultLBreak = 20 * 60; // seconds
int defaultSBreak = 5 * 60; // seconds
